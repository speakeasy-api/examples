/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { publicationsCreatePublication } from "../../funcs/publicationsCreatePublication.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreatePublicationRequest$inboundSchema,
};

export const tool$publicationsCreatePublication: ToolDefinition<typeof args> = {
  name: "publications-create-publication",
  description: `Create a new publication

Add a new publication to the store`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await publicationsCreatePublication(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
