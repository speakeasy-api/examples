/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { publicationsListPublications } from "../../funcs/publicationsListPublications.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$publicationsListPublications: ToolDefinition = {
  name: "publications-list-publications",
  description: `List all publications

Get a list of all publications in the store`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await publicationsListPublications(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
