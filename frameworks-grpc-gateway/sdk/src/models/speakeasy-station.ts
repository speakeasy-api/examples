/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * A train station.
 */
export type SpeakeasyStation = {
  /**
   * The address of the station.
   */
  address: string;
  /**
   * The country code of the station.
   */
  countryCode: string;
  /**
   * Unique identifier for the station.
   */
  id: string;
  /**
   * The name of the station.
   */
  name: string;
  /**
   * The timezone of the station in IANA format.
   */
  timezone?: string | undefined;
};

/** @internal */
export const SpeakeasyStation$inboundSchema: z.ZodMiniType<
  SpeakeasyStation,
  unknown
> = z.object({
  address: types.string(),
  countryCode: types.string(),
  id: types.string(),
  name: types.string(),
  timezone: types.optional(types.string()),
});

export function speakeasyStationFromJSON(
  jsonString: string,
): SafeParseResult<SpeakeasyStation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SpeakeasyStation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SpeakeasyStation' from JSON`,
  );
}
