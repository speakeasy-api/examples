/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getOrdersOrdersGet } from "../../funcs/getOrdersOrdersGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOrdersOrdersGetRequest$inboundSchema,
};

export const tool$getOrdersOrdersGet: ToolDefinition<typeof args> = {
  name: "get-orders-orders-get",
  description: `Get Orders

Retrieve all coffee orders.
If 'coffee_type' is provided, returns orders matching that coffee type.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await getOrdersOrdersGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
