/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { coffeeTypesUpdate } from "../../funcs/coffeeTypesUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateCoffeeTypeRequest$inboundSchema,
};

export const tool$coffeeTypesUpdate: ToolDefinition<typeof args> = {
  name: "coffee-types_update",
  description: `Update Coffee Type

Update an existing coffee type.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await coffeeTypesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
