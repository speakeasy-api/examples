/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { coffeeTypesGetById } from "../../funcs/coffeeTypesGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCoffeeTypeRequest$inboundSchema,
};

export const tool$coffeeTypesGetById: ToolDefinition<typeof args> = {
  name: "coffee-types_get-by-id",
  description: `Get Coffee Type

Retrieve a specific coffee type by its ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await coffeeTypesGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
