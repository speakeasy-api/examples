/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ordersGetById } from "../../funcs/ordersGetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOrderRequest$inboundSchema,
};

export const tool$ordersGetById: ToolDefinition<typeof args> = {
  name: "orders_get-by-id",
  description: `Get Order

Retrieve a specific coffee order by its ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ordersGetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
