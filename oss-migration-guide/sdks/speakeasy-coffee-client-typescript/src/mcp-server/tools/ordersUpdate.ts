/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ordersUpdate } from "../../funcs/ordersUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateOrderRequest$inboundSchema,
};

export const tool$ordersUpdate: ToolDefinition<typeof args> = {
  name: "orders_update",
  description: `Update Order

Update an existing coffee order.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ordersUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
