/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { coffeeTypesCreate } from "../../funcs/coffeeTypesCreate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CoffeeType$inboundSchema,
};

export const tool$coffeeTypesCreate: ToolDefinition<typeof args> = {
  name: "coffee-types_create",
  description: `Create Coffee Type

Create a new coffee type.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await coffeeTypesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
