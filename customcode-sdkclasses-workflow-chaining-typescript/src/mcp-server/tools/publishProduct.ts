/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { publishProduct } from "../../funcs/publishProduct.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PublishProductRequest$inboundSchema,
};

export const tool$publishProduct: ToolDefinition<typeof args> = {
  name: "publish-product",
  description: `Publish a product`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await publishProduct(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
