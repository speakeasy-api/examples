/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectionsQuery } from "../../funcs/collectionsQuery.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CollectionQueryRequest$inboundSchema,
};

export const tool$collectionsQuery: ToolDefinition<typeof args> = {
  name: "collections_query",
  description:
    `Query a collection in a variety of ways, including vector search, metadata filtering, and full-text search`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectionsQuery(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
