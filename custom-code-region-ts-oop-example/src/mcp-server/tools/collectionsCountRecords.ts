/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectionsCountRecords } from "../../funcs/collectionsCountRecords.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CollectionCountRequest$inboundSchema,
};

export const tool$collectionsCountRecords: ToolDefinition<typeof args> = {
  name: "collections_count-records",
  description: `Retrieves the number of records in a collection.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectionsCountRecords(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
