/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { databasesDelete } from "../../funcs/databasesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteDatabaseRequest$inboundSchema,
};

export const tool$databasesDelete: ToolDefinition<typeof args> = {
  name: "databases_delete",
  description: `Deletes a specific database.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await databasesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
