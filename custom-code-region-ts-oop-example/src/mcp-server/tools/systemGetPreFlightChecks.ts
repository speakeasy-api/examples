/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { systemGetPreFlightChecks } from "../../funcs/systemGetPreFlightChecks.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$systemGetPreFlightChecks: ToolDefinition = {
  name: "system_get-pre-flight-checks",
  description: `Pre-flight checks endpoint reporting basic readiness info.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await systemGetPreFlightChecks(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
