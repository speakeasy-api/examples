/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { databasesCreate } from "../../funcs/databasesCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateDatabaseRequest$inboundSchema,
};

export const tool$databasesCreate: ToolDefinition<typeof args> = {
  name: "databases_create",
  description: `Creates a new database for a given tenant.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await databasesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
