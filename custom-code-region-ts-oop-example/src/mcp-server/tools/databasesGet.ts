/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { databasesGet } from "../../funcs/databasesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetDatabaseRequest$inboundSchema,
};

export const tool$databasesGet: ToolDefinition<typeof args> = {
  name: "databases_get",
  description: `Retrieves a specific database by name.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await databasesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
