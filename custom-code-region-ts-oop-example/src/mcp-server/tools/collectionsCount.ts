/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectionsCount } from "../../funcs/collectionsCount.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CountCollectionsRequest$inboundSchema,
};

export const tool$collectionsCount: ToolDefinition<typeof args> = {
  name: "collections_count",
  description: `Retrieves the total number of collections in a given database.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectionsCount(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
