/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectionsList } from "../../funcs/collectionsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCollectionsRequest$inboundSchema,
};

export const tool$collectionsList: ToolDefinition<typeof args> = {
  name: "collections_list",
  description: `Lists all collections in the specified database.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectionsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
