/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { authGetIdentity } from "../../funcs/authGetIdentity.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$authGetIdentity: ToolDefinition = {
  name: "auth_get-identity",
  description: `Retrieves the current user's identity, tenant, and databases.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await authGetIdentity(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
