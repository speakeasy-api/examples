/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectionsUpdateById } from "../../funcs/collectionsUpdateById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CollectionUpdateRequest$inboundSchema,
};

export const tool$collectionsUpdateById: ToolDefinition<typeof args> = {
  name: "collections_update-by-id",
  description: `Updates records in a collection by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectionsUpdateById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
