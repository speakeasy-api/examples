/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectionsUpsert } from "../../funcs/collectionsUpsert.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CollectionUpsertRequest$inboundSchema,
};

export const tool$collectionsUpsert: ToolDefinition<typeof args> = {
  name: "collections_upsert",
  description:
    `Upserts records in a collection (create if not exists, otherwise update).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectionsUpsert(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
