/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { orderUpdateOrder } from "../../funcs/orderUpdateOrder.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateOrderRequest$inboundSchema,
};

export const tool$orderUpdateOrder: ToolDefinition<typeof args> = {
  name: "order-update-order",
  description: `Update Order

Update an order`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await orderUpdateOrder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
