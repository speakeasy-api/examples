/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { burgerCreateBurger } from "../../funcs/burgerCreateBurger.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.BurgerCreate$inboundSchema,
};

export const tool$burgerCreateBurger: ToolDefinition<typeof args> = {
  name: "burger-create-burger",
  description: `Create Burger

Create a burger`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await burgerCreateBurger(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
