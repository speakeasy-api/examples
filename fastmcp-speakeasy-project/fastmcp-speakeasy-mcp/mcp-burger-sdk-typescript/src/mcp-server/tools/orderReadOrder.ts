/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { orderReadOrder } from "../../funcs/orderReadOrder.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ReadOrderRequest$inboundSchema,
};

export const tool$orderReadOrder: ToolDefinition<typeof args> = {
  name: "order-read-order",
  description: `Read Order

Read an order`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await orderReadOrder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
