/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { burgerDeleteBurger } from "../../funcs/burgerDeleteBurger.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteBurgerRequest$inboundSchema,
};

export const tool$burgerDeleteBurger: ToolDefinition<typeof args> = {
  name: "burger-delete-burger",
  description: `Delete Burger

Delete a burger`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await burgerDeleteBurger(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
