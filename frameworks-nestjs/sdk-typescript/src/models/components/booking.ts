/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Booking = {
  /**
   * Booking identifier.
   */
  id: string;
  /**
   * Identifier of the booked trip.
   */
  tripId: string;
  /**
   * Passenger full name.
   */
  passengerName: string;
  /**
   * Whether the traveler has a bicycle.
   */
  hasBicycle: boolean;
  /**
   * Whether the traveler has a dog.
   */
  hasDog: boolean;
};

/** @internal */
export const Booking$inboundSchema: z.ZodType<Booking, z.ZodTypeDef, unknown> =
  z.object({
    id: z.string(),
    trip_id: z.string(),
    passenger_name: z.string(),
    has_bicycle: z.boolean(),
    has_dog: z.boolean(),
  }).transform((v) => {
    return remap$(v, {
      "trip_id": "tripId",
      "passenger_name": "passengerName",
      "has_bicycle": "hasBicycle",
      "has_dog": "hasDog",
    });
  });

export function bookingFromJSON(
  jsonString: string,
): SafeParseResult<Booking, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Booking$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Booking' from JSON`,
  );
}
