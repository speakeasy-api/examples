/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Station = {
  /**
   * Unique identifier for the station.
   */
  id: string;
  /**
   * Human-readable station name.
   */
  name: string;
  /**
   * Full street address.
   */
  address: string;
  /**
   * ISO country code.
   */
  countryCode: string;
  /**
   * IANA timezone.
   */
  timezone: string;
};

/** @internal */
export const Station$inboundSchema: z.ZodType<Station, z.ZodTypeDef, unknown> =
  z.object({
    id: z.string(),
    name: z.string(),
    address: z.string(),
    country_code: z.string(),
    timezone: z.string(),
  }).transform((v) => {
    return remap$(v, {
      "country_code": "countryCode",
    });
  });

export function stationFromJSON(
  jsonString: string,
): SafeParseResult<Station, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Station$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Station' from JSON`,
  );
}
