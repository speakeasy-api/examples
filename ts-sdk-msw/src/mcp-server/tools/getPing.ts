/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getPing } from "../../funcs/getPing.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPingRequestBody$inboundSchema.optional(),
};

export const tool$getPing: ToolDefinition<typeof args> = {
  name: "get-ping",
  description: `Ping endpoint

Returns a pong response`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await getPing(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
