/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type Station = {
  id: string;
  name: string;
  address: string;
  countryCode: string;
  timezone: string;
};

/** @internal */
export const Station$inboundSchema: z.ZodType<Station, z.ZodTypeDef, unknown> =
  z.object({
    id: z.string(),
    name: z.string(),
    address: z.string(),
    country_code: z.string(),
    timezone: z.string(),
  }).transform((v) => {
    return remap$(v, {
      "country_code": "countryCode",
    });
  });

/** @internal */
export type Station$Outbound = {
  id: string;
  name: string;
  address: string;
  country_code: string;
  timezone: string;
};

/** @internal */
export const Station$outboundSchema: z.ZodType<
  Station$Outbound,
  z.ZodTypeDef,
  Station
> = z.object({
  id: z.string(),
  name: z.string(),
  address: z.string(),
  countryCode: z.string(),
  timezone: z.string(),
}).transform((v) => {
  return remap$(v, {
    countryCode: "country_code",
  });
});

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace Station$ {
  /** @deprecated use `Station$inboundSchema` instead. */
  export const inboundSchema = Station$inboundSchema;
  /** @deprecated use `Station$outboundSchema` instead. */
  export const outboundSchema = Station$outboundSchema;
  /** @deprecated use `Station$Outbound` instead. */
  export type Outbound = Station$Outbound;
}

export function stationToJSON(station: Station): string {
  return JSON.stringify(Station$outboundSchema.parse(station));
}

export function stationFromJSON(
  jsonString: string,
): SafeParseResult<Station, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Station$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Station' from JSON`,
  );
}
