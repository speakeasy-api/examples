/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lapsListLaps } from "../../funcs/lapsListLaps.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListLapsRequest$inboundSchema,
};

export const tool$lapsListLaps: ToolDefinition<typeof args> = {
  name: "laps-list-laps",
  description: `Get all laps for a driver

Retrieve all lap records for a specific driver`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lapsListLaps(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
