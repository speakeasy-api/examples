/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { driversGetDriver } from "../../funcs/driversGetDriver.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetDriverRequest$inboundSchema,
};

export const tool$driversGetDriver: ToolDefinition<typeof args> = {
  name: "drivers-get-driver",
  description: `Get a specific driver

Retrieve details for a specific racing driver by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await driversGetDriver(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
