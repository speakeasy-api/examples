/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lapsDeleteLap } from "../../funcs/lapsDeleteLap.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteLapRequest$inboundSchema,
};

export const tool$lapsDeleteLap: ToolDefinition<typeof args> = {
  name: "laps-delete-lap",
  description: `Delete a lap record

Delete a specific lap record for a driver`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lapsDeleteLap(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
