/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { driversListDrivers } from "../../funcs/driversListDrivers.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$driversListDrivers: ToolDefinition = {
  name: "drivers-list-drivers",
  description: `Get all drivers

Retrieve a list of all racing drivers in the database`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await driversListDrivers(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
